# # .env.test

# # Environment mode
# NODE_ENV=test
# PORT=5001   # different port to avoid collision with dev

# # MongoDB URI
# # Option 1: local test database
# MONGO_URI=mongodb://localhost:27017/travelstrem_test

# # Option 2: (recommended for CI) use in-memory MongoDB (mongodb-memory-server)
# # In that case, you won’t need this MONGO_URI, your test runner spins up DB dynamically.

# # JWT secrets (use throwaway/random strings for test)
# JWT_ACCESS_SECRET=test_access_secret_random
# JWT_REFRESH_SECRET=test_refresh_secret_random

# # Token expiration (short expiry is useful in tests to simulate refresh flow)
# ACCESS_TOKEN_EXPIRES_IN=5m
# REFRESH_TOKEN_EXPIRES_IN=1h

# # Frontend origins (rarely needed in tests, but keep minimal)
# FRONTENDS=http://localhost:3000

# # Disable external integrations for tests (if any)
# SEND_EMAILS=false
# ENABLE_ANALYTICS=false
